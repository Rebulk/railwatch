// Shared marketing copy so the landing hero and the auth split-layout
// tagline panel say the same thing.
// The headline is "Monitoring Rails deserves."; its last word takes brand
// gold in the hero, the auth panel, and the social preview image alike.
export const HERO_HEADLINE_LEAD = "Monitoring Rails "
export const HERO_HEADLINE_ACCENT = "deserves."
export const HERO_SUBHEAD =
  "First-class monitoring for Rails. Every request, job, query, and exception, linked into one timeline."
